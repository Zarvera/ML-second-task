mixin color-display(color, colorName, colorCode)
    .color-display
        div(class='color-display__color-block color-block' + color)
        .color-display__text-block
            h2.color-display__color-name #{colorName}
            p.color-display__color-code #{colorCode}
//-
    'color' is a class _modifier for 'color-block' element.
    'colorName' is a text to be rendered as a the name of the displayed color.
    'colorCode' is a text to be rendered as the colors hexadecimal. 